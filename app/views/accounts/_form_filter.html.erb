<%= form_tag(account_transactions_path, :method => "get") do %>
	<div class="row">
		<%= hidden_field_tag 'origin_account_id', @account.id %>
		<div class="col-auto">				
      <%= text_field_tag 'start_date', params[:start_date], placeholder: "Data início", class: 'form-control' %>
		</div>
		<div class="col-auto">		
			 <%= text_field_tag 'end_date', params[:end_date], placeholder: "Data fim", class: 'form-control' %>
		</div>
		<div class="col-auto">
			<%= submit_tag "Filtrar", class: 'btn btn-primary' %>
		</div>
	</div>
<% end %>
